# =============================================================================
# Environment Variables for MySQL Container
# =============================================================================
# Copy this file to .env and update with your actual passwords
# Command: cp .env.example .env
# =============================================================================

# MySQL Root Password
# WARNING: Change this to a secure password!
MYSQL_ROOT_PASSWORD=rootpassword

# Default Database Name (both slmdb and slmDev will be created via init script)
MYSQL_DATABASE=slmdb

# Application Database User
MYSQL_USER=slm_user

# Application Database Password
# WARNING: Change this to a secure password!
# This must match the password in application-dev.properties
MYSQL_PASSWORD=slm_password

# =============================================================================
# INSTRUCTIONS:
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and set secure passwords
# 3. Update src/main/resources/application-dev.properties with same password
# 4. Start container: podman-compose up -d
# 5. Run application: ./gradlew bootRun --args="--spring.profiles.active=dev"
# =============================================================================

# IMPORTANT NOTES:
# - The .env file is gitignored to prevent password leaks
# - Both slmdb and slmDev databases will be created automatically
# - The init-db.sql script runs only on first container startup
# - If you change passwords, you must recreate the container and volume
# =============================================================================
