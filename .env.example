# =============================================================================
# MYSQL CONFIGURATION
# =============================================================================
MYSQL_ROOT_PASSWORD=your-secure-root-password-here
MYSQL_DATABASE=slmdb
MYSQL_USER=slm_user
MYSQL_PASSWORD=your-secure-user-password-here

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# IMPORTANT: Change this to a strong, unique secret in production!
# Generate a secure secret with: openssl rand -base64 64
JWT_SECRET=your-very-secure-secret-key-that-is-at-least-256-bits-long-for-hs256-algorithm-please-change-this-in-production
JWT_EXPIRATION=86400000

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Update this to your domain/IP address for production
# Examples:
#   - Local: http://localhost:3000
#   - Production: https://yourdomain.com
APP_UPLOAD_URL_PREFIX=http://localhost:3000

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins
# Supports wildcards: https://*.yourdomain.com matches all subdomains
# IMPORTANT: Include your server IP address if accessing via IP (e.g., during initial setup)
# Examples:
#   - Single domain: https://yourdomain.com
#   - Multiple domains: https://yourdomain.com,https://app.yourdomain.com
#   - Wildcard: https://*.yourdomain.com,https://yourdomain.com
#   - With server IP: https://yourdomain.com,http://YOUR-SERVER-IP:3000,http://YOUR-SERVER-IP
CORS_ALLOWED_ORIGINS=https://biedle.com,http://biedle.com,https://*.biedle.com,http://72.62.146.133:3000,http://72.62.146.133

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update all passwords and secrets with secure values
# 3. For production, set APP_UPLOAD_URL_PREFIX to your domain
# 4. Build and start: docker-compose up -d
# 5. View logs: docker-compose logs -f
#
# IMPORTANT NOTES:
# - The .env file is gitignored to prevent secrets from being committed
# - Never commit the .env file to version control
# - For production, use strong passwords and a unique JWT secret
# =============================================================================
